version: '3.9'
services:
  web:
    build: ./web
    ports:
      - "5000:5000"

    #volumes:
    #  - .:/code

  api:
    build: ./api
    ports:
      - "6050:6050"

    #volumes:
    #  - .:/code

  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: example

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

